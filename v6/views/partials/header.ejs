<!DOCTYPE html>
<html>
    <head>
        <title>YelpCamp</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/app.css">
    </head>
    <body>
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" href="/"><i class="glyphicon glyphicon-tent"></i> YelpCamp</a>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav navbar-right" id="user">
                        <% if(!currentUser){ %>
                            <li class="link"><a href="/login"><i class="glyphicon glyphicon-log-in"></i> Login</a></li>
                            <li class="link"><a href="/register"><i class="glyphicon glyphicon-pencil"></i> Sign Up</a></li>
                        <% } else { %>
                            <li class-"link"><a href="#"><i class="glyphicon glyphicon-user"> <%= currentUser.username %></i></a></li>
                            <li class="link"><a href="/logout"><i class="glyphicon glyphicon-log-out"></i> Logout</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>